# OpenCode Integration Notes

This folder tracks the OpenCode concepts to integrate next:

- Build/plan dual-agent pattern (planner vs executor).
- Session persistence and command templates.
- LSP integration for code intelligence (future).

Config mapping will live in `multi_agent_framework/config/agents.yaml` and `multi_agent_framework/config/models.yaml`.
